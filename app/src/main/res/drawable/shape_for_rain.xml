<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape>
            <solid android:color="@android:color/darker_gray" /> <!-- Arka plan rengi -->
            <corners android:radius="500dp" /> <!-- Köşe yarıçapı -->
        </shape>
    </item>
    <item android:top="0dp" android:bottom="0dp" android:left="0dp" android:right="0dp">
        <shape>
            <solid android:color="@android:color/darker_gray" /> <!-- İç arka plan rengi -->
            <corners android:radius="500dp" /> <!-- İç köşe yarıçapı -->
        </shape>
    </item>
</layer-list>